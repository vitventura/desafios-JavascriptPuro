const nome = prompt("qual o seu nome?")
const sobreNome = prompt("qual o seu sobrenome?")
const campoDeEstudo = prompt("qual o seu campo de estudo?")
const aniversario = prompt("qual sua data de aniversario?")

alert(
    "\n primeiro nome:" + nome +
    "\n sobreNome:" + sobreNome +
    "\n campo De Estudo:" + campoDeEstudo +
    "\n data de aniversario:" + aniversario 
)
/*\n pula uma linha */

const numeroUm = parseFloat(prompt("informe o primeiro numero"))
const numeroDois = parseFloat(prompt("informe o segundo numero"))

const soma = numeroUm + numeroDois
const subtracao = numeroUm - numeroDois
const divisao =  numeroUm * numeroDois
const multiplicaçao = numeroUm / numeroDois

alert("\nsoma:" + soma +
    "\nsoma:" + subtracao +
    "\nsoma:" + divisao +
    "\nsoma:" + multiplicaçao
)

